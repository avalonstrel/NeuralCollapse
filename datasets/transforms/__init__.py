# Now just import from pytorch, add custom operation if need

from torchvision.transforms import (Compose, CenterCrop, ColorJitter, Lighting, Normalize, Pad,
                         RandomCrop, RandomErasing, RandomFlip,
                         RandomGrayscale, RandomResizedCrop, Resize)

from 
__all__ = [
     'Compose', 'CenterCrop', 'ColorJitter', 'Lighting', 'Normalize', 'Pad',
     'RandomCrop', 'RandomErasing', 'RandomFlip',
     'RandomGrayscale', 'RandomResizedCrop', 'Resize'
]

